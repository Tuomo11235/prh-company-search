/**
 * Shared constants used by both the UI and the search logic, kept in one
 * place so they can never drift apart (e.g. the radius slider's max value
 * and the server-side clamp enforced before searching).
 */
export const MAX_RADIUS_KM = 10;
export const MIN_RADIUS_KM = 0;
